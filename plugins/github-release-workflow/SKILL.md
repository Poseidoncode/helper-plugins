# SKILL: github-release-workflow

## Description
Reads structured release evidence (`release-evidence.json`), constructs formatted markdown release notes, verifies tag/checksum consistency, and creates GitHub Release drafts safely (with draft flag enabled by default).
